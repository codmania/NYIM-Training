Hi <%= user.first_name %>

Your class '<%= course.name %> has the following signups:
<% course.attendants.each do |student| %>
<%= student.full_name %> - <%= student.email %> - <%= phone = student.phone_number ? student.phone_number.to_s : 'no phone' %>
<% end %>